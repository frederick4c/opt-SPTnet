# Reproducibility lock for opt-SPTnet.
#
# These are the resolved versions of the runtime and test dependency closure in
# the reference environment the package is developed and tested against
# (CPython 3.12). Use them for an exact, deterministic install:
#
#     pip install -e ".[test]" -c constraints.txt
#
# The Dockerfile and CI install without this file so they exercise the floors
# declared in pyproject.toml; pass `-c constraints.txt` when you need the exact
# tested set instead. Regenerate from a clean environment with:
#
#     pip install -e ".[test]" && pip freeze > constraints.txt
#
# then prune editable/local entries.

# direct runtime dependencies
h5py==3.16.0
matplotlib==3.10.9
numpy==2.4.6
positional-encodings==6.0.4
scipy==1.17.1
tifffile==2026.5.15
torch==2.12.0
tqdm==4.67.3

# test dependency
pytest==9.0.3

# key transitive dependencies (pinned for determinism)
contourpy==1.3.3
cycler==0.12.1
filelock==3.29.0
fonttools==4.63.0
fsspec==2026.4.0
iniconfig==2.3.0
Jinja2==3.1.6
kiwisolver==1.5.0
MarkupSafe==3.0.3
networkx==3.6.1
packaging==26.2
pillow==12.2.0
pluggy==1.6.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
sympy==1.14.0
